# Workflow guidelines

Updated 2022-05-11 by Clément

- Use VSCode with GitHub integration (eventually the with the `GitLens` extension)
- Look at the `Remote SSH` extension to run code directly on the server